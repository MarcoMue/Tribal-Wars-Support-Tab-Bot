name: Build and Release

on:
  push:
    branches: [main]
    tags: ["v*"]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Build executable
        run: |
          python -m PyInstaller StammGUI.spec

      - name: Upload build artifacts
        uses: actions/upload-artifact@v3
        with:
          name: StammGui-Windows
          path: dist/StammGui.exe
          retention-days: 30

      - name: Create Release (on tag push)
        if: startsWith(github.ref, 'refs/tags/')
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ github.ref }}
          release_name: Release ${{ github.ref }}
          body: |
            🚀 **Tribal Wars Support Tab Bot - Release ${{ github.ref }}**

            ## 📦 Download
            - Windows: `StammGui.exe`

            ## 🎯 Features
            - SOS-Anfragen parsen
            - Optimale Tab-Berechnung
            - Export für DS Ultimate

            ## 🔧 Installation
            1. Download `StammGui.exe`
            2. Ausführen - keine weitere Installation nötig!

            ## 📋 Changelog
            - See commit history for detailed changes

          draft: false
          prerelease: false

      - name: Upload Release Asset
        if: startsWith(github.ref, 'refs/tags/')
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: dist/StammGui.exe
          asset_name: StammGui.exe
          asset_content_type: application/octet-stream
